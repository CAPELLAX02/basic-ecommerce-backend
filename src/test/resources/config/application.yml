spring:
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true

  mail:
    # localhost does not need to be overwritten here
    port: 2025
    username: test
    password: test
